Стек технологий
Основной стек
PostgreSQL — основная БД.

Python — основной язык.

FastAPI — REST API.

SQLAlchemy — ORM.

Streamlit — web-клиент.

PySide6 — desktop-клиент.

pandas — таблицы и агрегации.

numpy — математика.

matplotlib — графики.

scikit-learn — ML-модели.

Cursor — среда разработки.

Дополнительно
Redis — кэш и очереди.

Celery — фоновые задачи.

Alembic — миграции БД.

Pydantic — валидация данных.

Uvicorn — сервер для разработки.

Gunicorn — сервер для production.

psycopg2 — драйвер PostgreSQL.

python-jose — JWT-токены.

passlib + bcrypt — хэширование паролей.

openpyxl — Excel.

reportlab — PDF.

httpx — запросы к API из desktop.

SQLite — локальный кэш desktop-клиента.

Docker — контейнеры.

Docker Compose — локальная разработка.

Nginx — reverse proxy и HTTPS.

Git — контроль версий.

GitHub — хранение репозитория.

ruff — линтер и форматтер.

pytest — тестирование.

Позже
React — полноценный web-клиент.

Kubernetes — production для SaaS.

Prometheus + Grafana — мониторинг.

Loki — логи.

Sentry — отслеживание ошибок.

MLflow — версионирование ML-моделей.

plotly — интерактивные графики.
